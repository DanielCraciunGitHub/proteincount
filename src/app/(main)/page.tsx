import { FormComponent } from "@/components/FormStateMachine";

// TODO redirect to /questions/personal if the user is not logged in
export default function ProfilePage() {
  return (
    <div>
      <FormComponent />
    </div>
  );
}
